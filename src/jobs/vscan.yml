description: >
  This job will generate vulnarability scan report of a given container.
# What will this job do?

docker:
  - image: 'cimg/base:stable'

parameters:
  imageName:
    type: string
    default: "node"
    description: "Container to scan"
steps:
  - vscan:
      imageName: << parameters.imageName >>
