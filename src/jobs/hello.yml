description: >
  Sample description
# What will this job do?

executor: default

parameters:
  imageName:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - xray:
      imageName: << parameters.imageName >>
