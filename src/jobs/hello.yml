description: >
  Sample description
# What will this job do?

executor: default

parameters:
  imageName:
    type: string
    default: "node"
    description: "Container to scan"
steps:
  - xray:
      imageName: << parameters.imageName >>
